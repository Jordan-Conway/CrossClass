#ifndef TOKEN_VISIBILITY
#define TOKEN_VISIBILITY

// Defines the visibility of an object or detail of an object
enum Visibility {
    VISIBILITY_PUBLIC,
    VISIBILITY_PRIVATE,
    VISIBILITY_INTERNAL,
    VISIBILITY_NOT_SET
};

#endif
